<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @beer.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @beer.content %>
</p>

<%= link_to 'Edit', edit_beer_path(@beer) %> |
<%= link_to 'Back', beers_path %>
